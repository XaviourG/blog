import { ReactElement } from "react";

const ParallaxQuotePanel = (): ReactElement => {

  return (
    <div style={{
      height: '100vh',
      width: '100vw',
    }}>
      Wow this is a really cool quote
    </div>
  );
};

export default ParallaxQuotePanel;